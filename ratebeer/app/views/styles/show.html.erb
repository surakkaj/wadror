<p id="notice"><%= notice %></p>

<p>
 <h1> <strong><%= @style.style %></strong></h1>
  <%= @style.desc %>
</p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Beer</th>
      <th></th>
      <th>Brewery</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @style.beers.each do |s| %>
      <tr>
        
        <td><%= link_to s.name, s %></td>
        <td>by</td>
        <td><%= link_to s.brewery, s.brewery %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>
<%= edit_and_destroy_buttons(@style) %>

